import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  iconStyle: {
    color: 'white',
    fontSize: 30,
    borderColor: 'white',
    paddingRight: 10
  }
});

export default styles;